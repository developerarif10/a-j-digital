/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['placehold.co', 'www.sannewijbenga.com'], // Added source for potential remote images
  },
}

module.exports = nextConfig